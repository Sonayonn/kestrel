[package]
name = "kestrel"
version = "0.1.0"
description = "Trustless private conditional-order execution engine for Solana perps"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "kestrel"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }
ephemeral-rollups-sdk = { version = "0.14.3", features = ["anchor", "access-control"] }